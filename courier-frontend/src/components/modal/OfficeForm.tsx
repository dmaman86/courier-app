import { OfficeResponse } from "../../types";


interface OfficeFormProps {
    officeId: number | null;
    onSubmit: (role: OfficeResponse) => void;
}

export const OfficeForm = ({ roleId, onSubmit }: OfficeFormProps) => {


    return(
        <>
        </>
    )
}